/**
 * Module contains application TopBar container
 * @module ui/components/TagCloud/Container
 * @author Igor Ivanov
 */
import styled from 'styled-components';

const Container = styled.div`
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    justify-content: flex-start;
`;

export default Container;
