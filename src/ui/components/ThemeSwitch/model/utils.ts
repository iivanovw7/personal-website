/**
 * Module contains utils used in Theme Switch component
 * @module ui/components/ThemeSwitch/model/utils
 * @author Igor Ivanov
 */
import { DARK_THEME } from '../../../../config/constants';

export const isDarkTheme = (theme) => theme === DARK_THEME; // eslint-disable-line
